## R 最常用的 package

- readr:data read tools;
- dplyr:data clean tools;
- tidyr:data reshape tools;
- stringr:figure string tools;

## R regexp
grep
 - pattern 正则表达式;
 - vecotor strings 由字符串构成的向量;
 - value 逻辑值,是否显示被匹配的值;
 - fixed 逻辑值,是否只是匹配字面量还是正则表达式;
 - perl 逻辑值,匹配正则的规则是按照 perl 还是R本身;
